<form method="post" action="{{ path('app_detalle_presupuesto_delete', {'id': detalle_presupuesto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detalle_presupuesto.id) }}">
    <button class="btn">Delete</button>
</form>
