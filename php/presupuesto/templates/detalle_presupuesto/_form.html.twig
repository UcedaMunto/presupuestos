{# _form.html.twig para DetallePresupuestoType #}
{{ form_start(form) }}
<div class="container card border-secondary">
    <div class="form-group row">
        <div class="col-md-6">
            {{ form_label(form.presupuesto) }} <br>
            {{ form_widget(form.presupuesto) }}
        </div>
        <div class="col-md-6">
            {{ form_label(form.item) }} <br>
            {{ form_widget(form.item) }}
        </div>
    </div>
    <div class="form-group row">
        <div class="col-md-6">
            {{ form_label(form.cantidad) }} <br>
            {{ form_widget(form.cantidad) }}
        </div>
        <div class="col-md-6">
            {{ form_label(form.precio) }} <br>
            {{ form_widget(form.precio) }}
        </div>
    </div>
    <div class="form-group row mt-3">
        <div class="col-md-12 text-center">
            <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
