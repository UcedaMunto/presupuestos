{% extends 'base.html.twig' %}

{% block title %}TipoProducto{% endblock %}

{% block body %}
    <h1>TipoProducto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_producto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_producto.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_producto_index') }}">back to list</a>

    <a href="{{ path('app_tipo_producto_edit', {'id': tipo_producto.id}) }}">edit</a>

    {{ include('tipo_producto/_delete_form.html.twig') }}
{% endblock %}
