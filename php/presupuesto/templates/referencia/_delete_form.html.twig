<form method="post" action="{{ path('app_referencia_delete', {'id': referencium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ referencium.id) }}">
    <button class="btn">Delete</button>
</form>
