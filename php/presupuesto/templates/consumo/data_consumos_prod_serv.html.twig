    
    <div class="container mt-4">
        <h1 class="text-center mb-4">Consumos del Presupuesto </h1>

        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Tipo</th>
                        <th>Cantidad</th>
                        <th>Costo Consumo</th>
                        <th>Costo Unitario</th>
                        <th>Descripción</th>
                    </tr>
                </thead>
                <tbody>
                    {% for consumo in consumos %}
                        <tr>
                            <td>{{ consumo.id }}</td>
                            <td>{{ consumo.nombre }}</td>
                            <td>{{ consumo.tipo }}</td>
                            <td>{{ consumo.cantidad }}</td>
                            <td>{{ consumo.costo_consumo }}</td>
                            <td>{{ consumo.costo_prod_servicio }}</td>
                            <td>{{ consumo.descripcion }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="7" class="text-center text-muted">No hay consumos registrados para este presupuesto.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>