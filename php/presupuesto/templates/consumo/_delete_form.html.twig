<form method="post" action="{{ path('app_consumo_delete', {'id': consumo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consumo.id) }}">
    <button class="btn">Delete</button>
</form>
