<form method="post" action="{{ path('app_referencia_compra_delete', {'id': referencia_compra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ referencia_compra.id) }}">
    <button class="btn">Delete</button>
</form>
