{% extends 'base.html.twig' %}

{% block title %}EstadoPresupuesto index{% endblock %}

{% block body %}
    <h1>EstadoPresupuesto index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for estado_presupuesto in estado_presupuestos %}
            <tr>
                <td>{{ estado_presupuesto.id }}</td>
                <td>{{ estado_presupuesto.nombre }}</td>
                <td>
                    <a href="{{ path('app_estado_presupuesto_show', {'id': estado_presupuesto.id}) }}">show</a>
                    <a href="{{ path('app_estado_presupuesto_edit', {'id': estado_presupuesto.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_estado_presupuesto_new') }}">Create new</a>
{% endblock %}
