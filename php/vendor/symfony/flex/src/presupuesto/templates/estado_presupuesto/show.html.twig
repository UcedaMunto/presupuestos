{% extends 'base.html.twig' %}

{% block title %}EstadoPresupuesto{% endblock %}

{% block body %}
    <h1>EstadoPresupuesto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado_presupuesto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estado_presupuesto.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_estado_presupuesto_index') }}">back to list</a>

    <a href="{{ path('app_estado_presupuesto_edit', {'id': estado_presupuesto.id}) }}">edit</a>

    {{ include('estado_presupuesto/_delete_form.html.twig') }}
{% endblock %}
